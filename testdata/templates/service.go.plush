package <%= def.PackageName %>

<%= for (service) in def.Services { %>

type <%= service.Name %> interface {
<%= for (method) in service.Methods { %>
	<%= method.Name %>(<%= method.InputObject.TypeName %>) (*<%= method.OutputObject.TypeName %>, error)
<% } %>
}

<% } %>